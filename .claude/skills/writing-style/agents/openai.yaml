interface:
  display_name: "Writing Style — Remove AI Slop"
  short_description: "Remove AI slop and optionally match a writer’s voice"
  default_prompt: "Use $writing-style to remove AI slop from this draft while preserving its meaning; match my voice if samples or a profile are available, and return only the revised text."
